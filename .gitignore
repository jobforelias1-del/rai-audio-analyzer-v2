# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/*.app/
dist/
.venv/
venv/
# M5 cutover: the pre-rebuild Homebrew-drifted engine venv, parked for
# rollback until close-out (RC deletes), + the transient rebuild name.
.venv-old-homebrew/
.venv-rebuilt/

# PyInstaller
*.spec.bak

# Test / tooling
.pytest_cache/
.coverage
htmlcov/

# OS
.DS_Store

# Ground-truth WAV fixtures are large and not committed; drop them in here.
validation/fixtures/*.wav
validation/fixtures/*.aiff
validation/fixtures/*.flac
validation/fixtures/*.mp3

# Phase 0 (2026-07-06): regenerable PyInstaller work dir
build/RAIAudioAnalyzer/

# Learn-workflow fingerprint outputs (per-machine artifacts; canonical
# packaged default is fingerprints/drill.json, which IS tracked).
rai_analyzer/fingerprints/drill.bootstrap.json
rai_analyzer/fingerprints/drill.committed.json
rai_analyzer/fingerprints/drill.learned.json

# Phase 3 UI venv
.venv-v3/

# Phase 3 frozen-build outputs
dist-v3/
build/RAIv3-work/
