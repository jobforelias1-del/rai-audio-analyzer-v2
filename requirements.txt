# RAI Audio Analyzer — engine Python dependencies
#
# Versions are floors derived from what is installed in the reference
# environment (pip show output on 2026-05-29).  Use >= so that callers on
# newer interpreters do not have to pin an exact version.
#
# Core DSP + audio I/O
numpy>=2.4.6
scipy>=1.17.1
librosa>=0.11.0
soundfile>=0.13.1
pyloudnorm>=0.2.0

# numba is transitive (librosa → numba) but needs an explicit floor: modern
# numba caps its numpy compatibility, so a resolver chasing the newest numpy
# can backtrack onto an ancient uncapped numba whose build then fails on
# Python 3.14 (observed with uv 0.11 during the 2026-07-07 v3 cutover
# rebuild). The floor keeps the resolution in the verified family.
numba>=0.65

# macOS .app packaging (install on the build Mac only; not needed at runtime)
pyinstaller>=6.0
