# RAI Audio Analyzer v2 — Python dependencies
#
# Versions are floors derived from what is installed in the reference
# environment (pip show output on 2026-05-29).  Use >= so that callers on
# newer interpreters do not have to pin an exact version.
#
# Core DSP + audio I/O
numpy>=2.4.6
scipy>=1.17.1
librosa>=0.11.0
soundfile>=0.13.1
pyloudnorm>=0.2.0

# GUI (drag-drop interface)
matplotlib>=3.7
tkinterdnd2>=0.3

# macOS .app packaging (install on the build Mac only; not needed at runtime)
pyinstaller>=6.0
